#include "stdafx.h"
#include "Input.h"
#include "Managers.h"
#include "Player.h"
#include "Enum.h"

void CInput::Update()
{
	//if (Key->Key_Pressing(VK_RIGHT))
	//{
	//	//m_pHost->Set_SpeedX(10.f);
	//	m_pHost->SetState(STATE::MOVE);
	//	m_pHost->SetDir(DIR::RIGHT);
	//	// Speed
	//}

	//if (Key->Key_Pressing(VK_LEFT))
	//{
	//	m_pHost->SetState(STATE::MOVE);
	//	m_pHost->SetDir(DIR::LEFT);
	//}

	//if (Key->Key_Pressing(VK_UP))
	//{
	//	m_pHost->SetState(STATE::MOVE);
	//	m_pHost->SetDir(DIR::UP);
	//}

	//if (Key->Key_Pressing(VK_DOWN))
	//{
	//	m_pHost->SetState(STATE::MOVE);
	//	m_pHost->SetDir(DIR::DOWN);
	//}

	//if (Key->Key_Pressing(VK_SPACE))
	//{
	//	m_pHost->SetState(STATE::ATTACK);
	//}
}


CInput::~CInput()
{
}
